# Sentry DSN for browser error tracking and session replay.
# Copy this file to .env.local and fill in the value for local development.
# In Railway, set VITE_FRONTEND_SENTRY_DSN as a service environment variable —
# it is distinct from the backend's SENTRY_DSN and is injected at Docker build time.
VITE_FRONTEND_SENTRY_DSN=
